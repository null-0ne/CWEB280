{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run nearest project",
            "type": "node",
            "request": "launch",
            "cwd": "${fileDirname}",
            "program": "${workspaceFolder}/.vscode/find-app.js"
        }
    ]
}