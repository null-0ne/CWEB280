<html>
  <head>
    <title>{{title}}</title>
    <!-- CSS files links so all pages have the same CSS -->
    <!-- common JS that needs to be available on all pages -->
  </head>
  <body>
    {{{body}}}
    <!-- this is where "home.handlebars" will be injected -->
  </body>
</html>